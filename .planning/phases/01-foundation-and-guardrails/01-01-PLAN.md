---
phase: 01-foundation-and-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Unity/Packages/manifest.json
  - Unity/ProjectSettings/ProjectSettings.asset
  - Unity/ProjectSettings/EditorBuildSettings.asset
  - Unity/Assets/_Project/Scenes/Bootstrap.unity
  - .planning/phases/01-foundation-and-guardrails/01-BENCHMARK-ASSUMPTIONS.md
autonomous: true
requirements: [TECH-01]
user_setup: []
must_haves:
  truths:
    - "O projeto deixa de usar identidade de template e passa a carregar uma identidade propria de Ascend."
    - "A cena de entrada do build deixa de ser SampleScene e passa a ser um Bootstrap scene do projeto."
    - "As premissas de benchmark do slice ficam registradas com Galaxy A15 como baseline principal, Galaxy A10 como smoke target e target de 30 FPS."
  artifacts:
    - path: "Unity/Packages/manifest.json"
      provides: "Manifesto normalizado sem pacotes fora do escopo do slice"
      contains: "com.unity.inputsystem"
    - path: "Unity/ProjectSettings/ProjectSettings.asset"
      provides: "Player Settings sem identidade de template"
      contains: "productName: Ascend"
    - path: "Unity/ProjectSettings/EditorBuildSettings.asset"
      provides: "Build scene ownership pelo projeto"
      contains: "Assets/_Project/Scenes/Bootstrap.unity"
    - path: ".planning/phases/01-foundation-and-guardrails/01-BENCHMARK-ASSUMPTIONS.md"
      provides: "Contrato humano de benchmark da Fase 1"
      contains: "Samsung Galaxy A15"
    - path: "Unity/Assets/_Project/Scenes/Bootstrap.unity"
      provides: "Cena shell minima do projeto"
      min_lines: 20
  key_links:
    - from: "Unity/ProjectSettings/EditorBuildSettings.asset"
      to: "Unity/Assets/_Project/Scenes/Bootstrap.unity"
      via: "scene path in build settings"
      pattern: "Assets/_Project/Scenes/Bootstrap\\.unity"
---

<objective>
Normalizar o baseline do projeto Unity, remover heranca desnecessaria do template 2D/URP e registrar as premissas de benchmark do slice.

Purpose: criar uma base de projeto propria de Ascend antes que runtime, dados e testes comecem a depender de settings de template.
Output: manifesto enxuto, Player Settings normalizados, Bootstrap scene como entrada do build e benchmark assumptions documentadas.
</objective>

<execution_context>
@/home/gugabru9/.codex_sessions/pessoal/get-shit-done/workflows/execute-plan.md
@/home/gugabru9/.codex_sessions/pessoal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-guardrails/01-CONTEXT.md
@.planning/phases/01-foundation-and-guardrails/01-RESEARCH.md
@Unity/Packages/manifest.json
@Unity/Packages/packages-lock.json
@Unity/ProjectSettings/ProjectSettings.asset
@Unity/ProjectSettings/EditorBuildSettings.asset
@Unity/Assets/Scenes/SampleScene.unity
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim template packages to the slice baseline</name>
  <files>Unity/Packages/manifest.json</files>
  <read_first>.planning/phases/01-foundation-and-guardrails/01-CONTEXT.md, .planning/phases/01-foundation-and-guardrails/01-RESEARCH.md, Unity/Packages/manifest.json, Unity/Packages/packages-lock.json</read_first>
  <action>Update `Unity/Packages/manifest.json` to keep the Phase 1 baseline focused on the slice: preserve `com.unity.inputsystem`, `com.unity.render-pipelines.universal`, `com.unity.ugui`, `com.unity.test-framework` and the current 2D asset/import packages, but remove `com.unity.visualscripting`, `com.unity.multiplayer.center`, and `com.unity.collab-proxy`. Do not add Addressables, UI Toolkit runtime packages, DOTS, or multiplayer packages in this phase.</action>
  <verify>rg -n "com\\.unity\\.(visualscripting|multiplayer\\.center|collab-proxy)" Unity/Packages/manifest.json && exit 1 || true</verify>
  <acceptance_criteria>
    - `Unity/Packages/manifest.json` does not contain `com.unity.visualscripting`
    - `Unity/Packages/manifest.json` does not contain `com.unity.multiplayer.center`
    - `Unity/Packages/manifest.json` does not contain `com.unity.collab-proxy`
    - `Unity/Packages/manifest.json` contains `com.unity.inputsystem`
    - `Unity/Packages/manifest.json` contains `com.unity.ugui`
    - `Unity/Packages/manifest.json` contains `com.unity.render-pipelines.universal`
  </acceptance_criteria>
  <done>Manifesto deixa de carregar pacotes fora do escopo do slice e preserva apenas o baseline tecnico aprovado.</done>
</task>

<task type="auto">
  <name>Task 2: Replace template identity and record benchmark assumptions</name>
  <files>Unity/ProjectSettings/ProjectSettings.asset, .planning/phases/01-foundation-and-guardrails/01-BENCHMARK-ASSUMPTIONS.md</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/phases/01-foundation-and-guardrails/01-CONTEXT.md, Unity/ProjectSettings/ProjectSettings.asset</read_first>
  <action>Normalize `Unity/ProjectSettings/ProjectSettings.asset` so the project no longer looks like the Unity 2D template: replace `companyName: DefaultCompany` with `companyName: Ascend`, replace `productName: Unity` with `productName: Ascend`, and replace the template application identifier root `com.DefaultCompany.2D-URP` with the internal placeholder root `com.ascend.slice` for the generated player settings. Create `.planning/phases/01-foundation-and-guardrails/01-BENCHMARK-ASSUMPTIONS.md` with exact fields for `primary_device = Samsung Galaxy A15`, `secondary_smoke_device = Samsung Galaxy A10`, `target_frame_rate = 30`, `profiling_scenarios = boot-shell, hud-idle, combat-smoke`, and a note that A10 remains a compatibility smoke target rather than the budget owner.</action>
  <verify>rg -n "productName: Ascend|companyName: Ascend|com\\.ascend\\.slice" Unity/ProjectSettings/ProjectSettings.asset && rg -n "Samsung Galaxy A15|Samsung Galaxy A10|target_frame_rate = 30" .planning/phases/01-foundation-and-guardrails/01-BENCHMARK-ASSUMPTIONS.md</verify>
  <acceptance_criteria>
    - `Unity/ProjectSettings/ProjectSettings.asset` contains `companyName: Ascend`
    - `Unity/ProjectSettings/ProjectSettings.asset` contains `productName: Ascend`
    - `Unity/ProjectSettings/ProjectSettings.asset` contains `com.ascend.slice`
    - `.planning/phases/01-foundation-and-guardrails/01-BENCHMARK-ASSUMPTIONS.md` contains `primary_device = Samsung Galaxy A15`
    - `.planning/phases/01-foundation-and-guardrails/01-BENCHMARK-ASSUMPTIONS.md` contains `secondary_smoke_device = Samsung Galaxy A10`
    - `.planning/phases/01-foundation-and-guardrails/01-BENCHMARK-ASSUMPTIONS.md` contains `target_frame_rate = 30`
  </acceptance_criteria>
  <done>Ascend passa a ter identidade propria no Player Settings e o contrato de benchmark da fase fica explicito no repositorio.</done>
</task>

<task type="auto">
  <name>Task 3: Replace SampleScene with a stable Bootstrap scene</name>
  <files>Unity/ProjectSettings/EditorBuildSettings.asset, Unity/Assets/_Project/Scenes/Bootstrap.unity</files>
  <read_first>.planning/phases/01-foundation-and-guardrails/01-CONTEXT.md, Unity/ProjectSettings/EditorBuildSettings.asset, Unity/Assets/Scenes/SampleScene.unity</read_first>
  <action>Create `Unity/Assets/_Project/Scenes/Bootstrap.unity` as the sole build scene for Phase 1. The scene must be a stable shell owned by the project, free of template gameplay content, and safe to open before combat systems exist. Update `Unity/ProjectSettings/EditorBuildSettings.asset` so `Assets/_Project/Scenes/Bootstrap.unity` is the enabled build scene and `Assets/Scenes/SampleScene.unity` is no longer the active entry point.</action>
  <verify>test -f Unity/Assets/_Project/Scenes/Bootstrap.unity && rg -n "Assets/_Project/Scenes/Bootstrap\\.unity" Unity/ProjectSettings/EditorBuildSettings.asset && ! rg -n "Assets/Scenes/SampleScene\\.unity" Unity/ProjectSettings/EditorBuildSettings.asset</verify>
  <acceptance_criteria>
    - `Unity/Assets/_Project/Scenes/Bootstrap.unity` exists
    - `Unity/ProjectSettings/EditorBuildSettings.asset` contains `Assets/_Project/Scenes/Bootstrap.unity`
    - `Unity/ProjectSettings/EditorBuildSettings.asset` does not contain `Assets/Scenes/SampleScene.unity`
  </acceptance_criteria>
  <done>O build passa a entrar por uma cena Bootstrap do projeto, em vez de depender da SampleScene do template.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "com\\.unity\\.(visualscripting|multiplayer\\.center|collab-proxy)" Unity/Packages/manifest.json` returns no matches
- [ ] `rg -n "companyName: Ascend|productName: Ascend|com\\.ascend\\.slice" Unity/ProjectSettings/ProjectSettings.asset` finds all expected values
- [ ] `test -f Unity/Assets/_Project/Scenes/Bootstrap.unity` succeeds
- [ ] `rg -n "Assets/_Project/Scenes/Bootstrap\\.unity" Unity/ProjectSettings/EditorBuildSettings.asset` succeeds
</verification>

<success_criteria>

- All tasks completed
- O projeto deixa de parecer um template generico de Unity 2D
- O build entra por `Bootstrap.unity`
- O contrato de benchmark da fase esta registrado com Galaxy A15, Galaxy A10 e 30 FPS

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-guardrails/01-01-SUMMARY.md`
</output>
