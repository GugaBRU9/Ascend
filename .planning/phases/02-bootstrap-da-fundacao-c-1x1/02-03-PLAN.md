---
phase: 02-bootstrap-da-fundacao-c-1x1
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - tests/CMakeLists.txt
  - tests/bootstrap_smoke.cpp
autonomous: true
requirements:
  - ARCH-05
  - VAL-05
user_setup: []
must_haves:
  truths:
    - The phase ends with at least one real CTest test, not only wiring.
    - The smoke test includes the public headers from all four modules and checks concrete outputs.
    - The same test runs through the dev and asan-ubsan presets, proving the validation baseline is actionable.
  artifacts:
    - tests/CMakeLists.txt
    - tests/bootstrap_smoke.cpp
    - CMakePresets.json
  key_links:
    - tests/CMakeLists.txt registers `bootstrap_smoke` in CTest.
    - tests/bootstrap_smoke.cpp includes headers from domain, content, session, and adapters.
    - CTest runs under the preset names already documented in README.md and defined in CMakePresets.json.
---

<objective>
Fechar a baseline de testes e das configuracoes instrumentadas.

Purpose: provar que o bootstrap da `Phase 2` nao para em build/configure, mas ja entrega uma referencia pequena de validacao com `CTest` e sanitizers.
Output: `tests/CMakeLists.txt` e `tests/bootstrap_smoke.cpp`.
</objective>

<execution_context>
@$HOME/.codex_sessions/pessoal/get-shit-done/workflows/execute-plan.md
@$HOME/.codex_sessions/pessoal/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@CMakeLists.txt
@CMakePresets.json
@tests/CMakeLists.txt
@src/domain/include/ascend/domain/module_name.hpp
@src/content/include/ascend/content/module_name.hpp
@src/session/include/ascend/session/module_name.hpp
@src/adapters/include/ascend/adapters/text_adapter_name.hpp
@.planning/phases/02-bootstrap-da-fundacao-c-1x1/02-CONTEXT.md
@.planning/phases/02-bootstrap-da-fundacao-c-1x1/02-RESEARCH.md
@.planning/phases/02-bootstrap-da-fundacao-c-1x1/02-VALIDATION.md
</context>

<threat_model>
<trust_boundaries>
- A validacao desta phase precisa ser automatizada o bastante para nao depender de leitura subjetiva do build.
- O teste nao pode puxar frameworks extras nem antecipar `CombatState`; ele deve provar o bootstrap com o menor atrito possivel.
- O preset `asan-ubsan` precisa executar o mesmo caminho do smoke test, nao um caminho alternativo sem cobertura real.
</trust_boundaries>

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Repudiation | CTest baseline | mitigate | Registrar explicitamente `bootstrap_smoke` em `tests/CMakeLists.txt` e usar `ctest --preset dev` / `ctest --preset asan-ubsan` como verificacao. |
| T-02-08 | Tampering | Smoke test semantics | mitigate | Fazer o executavel incluir os quatro headers publicos e comparar retornos exatos: `domain`, `content`, `session`, `text-adapter`. |
| T-02-09 | Denial of Service | Validation loop | mitigate | Manter o teste em `assert` + `std::string_view`, sem framework externo e sem dependencia de runtime de combate ainda nao implementado. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Registrar um teste real no CTest do bootstrap</name>
  <files>tests/CMakeLists.txt</files>
  <read_first>tests/CMakeLists.txt, CMakeLists.txt, CMakePresets.json, src/domain/CMakeLists.txt, src/content/CMakeLists.txt, src/session/CMakeLists.txt, src/adapters/CMakeLists.txt, .planning/phases/02-bootstrap-da-fundacao-c-1x1/02-VALIDATION.md</read_first>
  <action>Substituir o placeholder de `tests/CMakeLists.txt` por wiring real: criar `add_executable(ascend_bootstrap_smoke bootstrap_smoke.cpp)`, definir `target_link_libraries(ascend_bootstrap_smoke PRIVATE ascend_build_options ascend_build_warnings ascend_domain ascend_content ascend_session ascend_adapters)` e registrar `add_test(NAME bootstrap_smoke COMMAND ascend_bootstrap_smoke)`. O arquivo deve continuar simples, sem framework externo e sem logica de descoberta automatica.</action>
  <verify>test -f tests/CMakeLists.txt && rg -n "add_executable\\(ascend_bootstrap_smoke bootstrap_smoke.cpp\\)|target_link_libraries\\(ascend_bootstrap_smoke PRIVATE ascend_build_options ascend_build_warnings ascend_domain ascend_content ascend_session ascend_adapters\\)|add_test\\(NAME bootstrap_smoke COMMAND ascend_bootstrap_smoke\\)" tests/CMakeLists.txt</verify>
  <acceptance_criteria>
    - tests/CMakeLists.txt contains `add_executable(ascend_bootstrap_smoke bootstrap_smoke.cpp)`
    - tests/CMakeLists.txt contains `target_link_libraries(ascend_bootstrap_smoke PRIVATE ascend_build_options ascend_build_warnings ascend_domain ascend_content ascend_session ascend_adapters)`
    - tests/CMakeLists.txt contains `add_test(NAME bootstrap_smoke COMMAND ascend_bootstrap_smoke)`
  </acceptance_criteria>
  <done>CTest passou a registrar um executavel de smoke real do bootstrap.</done>
</task>

<task type="auto">
  <name>Task 2: Criar smoke test pequeno e provar os presets dev e asan-ubsan</name>
  <files>tests/bootstrap_smoke.cpp</files>
  <read_first>tests/bootstrap_smoke.cpp, tests/CMakeLists.txt, src/domain/include/ascend/domain/module_name.hpp, src/content/include/ascend/content/module_name.hpp, src/session/include/ascend/session/module_name.hpp, src/adapters/include/ascend/adapters/text_adapter_name.hpp, CMakePresets.json, README.md</read_first>
  <action>Criar `tests/bootstrap_smoke.cpp` usando apenas `<cassert>` e `<string_view>`. Incluir exatamente `ascend/domain/module_name.hpp`, `ascend/content/module_name.hpp`, `ascend/session/module_name.hpp` e `ascend/adapters/text_adapter_name.hpp`. No `main`, validar com `assert` que `ascend::domain::module_name()` retorna `"domain"`, `ascend::content::module_name()` retorna `"content"`, `ascend::session::module_name()` retorna `"session"` e `ascend::adapters::text_adapter_name()` retorna `"text-adapter"`, finalizando com `return 0;`. Depois provar o loop rodando `cmake --preset dev && cmake --build --preset dev && ctest --preset dev --output-on-failure`, seguido de `cmake --preset asan-ubsan && cmake --build --preset asan-ubsan && ctest --preset asan-ubsan --output-on-failure`.</action>
  <verify>test -f tests/bootstrap_smoke.cpp && rg -n "ascend/domain/module_name\\.hpp|ascend/content/module_name\\.hpp|ascend/session/module_name\\.hpp|ascend/adapters/text_adapter_name\\.hpp|assert\\(ascend::domain::module_name\\(\\) == \"domain\"\\)|assert\\(ascend::adapters::text_adapter_name\\(\\) == \"text-adapter\"\\)" tests/bootstrap_smoke.cpp && cmake --preset dev && cmake --build --preset dev && ctest --preset dev --output-on-failure && cmake --preset asan-ubsan && cmake --build --preset asan-ubsan && ctest --preset asan-ubsan --output-on-failure</verify>
  <acceptance_criteria>
    - tests/bootstrap_smoke.cpp includes `ascend/domain/module_name.hpp`
    - tests/bootstrap_smoke.cpp includes `ascend/content/module_name.hpp`
    - tests/bootstrap_smoke.cpp includes `ascend/session/module_name.hpp`
    - tests/bootstrap_smoke.cpp includes `ascend/adapters/text_adapter_name.hpp`
    - tests/bootstrap_smoke.cpp contains `assert(ascend::domain::module_name() == "domain")`
    - tests/bootstrap_smoke.cpp contains `assert(ascend::content::module_name() == "content")`
    - tests/bootstrap_smoke.cpp contains `assert(ascend::session::module_name() == "session")`
    - tests/bootstrap_smoke.cpp contains `assert(ascend::adapters::text_adapter_name() == "text-adapter")`
    - `ctest --preset dev --output-on-failure` exits 0
    - `ctest --preset asan-ubsan --output-on-failure` exits 0
  </acceptance_criteria>
  <done>O bootstrap fecha com um teste real pequeno e com a baseline instrumentada passando.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f tests/CMakeLists.txt`
- [ ] `test -f tests/bootstrap_smoke.cpp`
- [ ] `rg -n "add_test\\(NAME bootstrap_smoke" tests/CMakeLists.txt` succeeds
- [ ] `rg -n "assert\\(ascend::domain::module_name\\(\\) == \"domain\"\\)|assert\\(ascend::adapters::text_adapter_name\\(\\) == \"text-adapter\"\\)" tests/bootstrap_smoke.cpp` succeeds
- [ ] `cmake --preset dev` succeeds
- [ ] `cmake --build --preset dev` succeeds
- [ ] `ctest --preset dev --output-on-failure` succeeds
- [ ] `cmake --preset asan-ubsan` succeeds
- [ ] `cmake --build --preset asan-ubsan` succeeds
- [ ] `ctest --preset asan-ubsan --output-on-failure` succeeds
</verification>

<success_criteria>

- All tasks completed
- Existe pelo menos um teste real pequeno registrado em `CTest`
- O mesmo smoke test roda em `dev` e `asan-ubsan`
- O estudante ganha uma referencia curta e compilavel para validar o bootstrap
  </success_criteria>

<output>
After completion, create `.planning/phases/02-bootstrap-da-fundacao-c-1x1/02-03-SUMMARY.md`
</output>
