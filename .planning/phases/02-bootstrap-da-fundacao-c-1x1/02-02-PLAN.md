---
phase: 02-bootstrap-da-fundacao-c-1x1
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/domain/CMakeLists.txt
  - src/domain/include/ascend/domain/module_name.hpp
  - src/domain/src/module_name.cpp
  - src/content/CMakeLists.txt
  - src/content/include/ascend/content/module_name.hpp
  - src/content/src/module_name.cpp
  - src/session/CMakeLists.txt
  - src/session/include/ascend/session/module_name.hpp
  - src/session/src/module_name.cpp
  - src/adapters/CMakeLists.txt
  - src/adapters/include/ascend/adapters/text_adapter_name.hpp
  - src/adapters/src/text_adapter_name.cpp
autonomous: true
requirements:
  - ARCH-06
user_setup: []
must_haves:
  truths:
    - The repository contains explicit domain, content, session, and adapters modules as separate targets.
    - Public headers live under include/ascend/... and each module compiles at least one real translation unit.
    - Dependency direction is visible in CMake: session consumes domain/content, adapters consume session, and no reverse link exists.
  artifacts:
    - src/domain/CMakeLists.txt
    - src/domain/include/ascend/domain/module_name.hpp
    - src/content/CMakeLists.txt
    - src/content/include/ascend/content/module_name.hpp
    - src/session/CMakeLists.txt
    - src/session/include/ascend/session/module_name.hpp
    - src/adapters/CMakeLists.txt
    - src/adapters/include/ascend/adapters/text_adapter_name.hpp
  key_links:
    - ascend_session links ascend_domain and ascend_content.
    - ascend_adapters links ascend_session.
    - Public header paths mirror module names under include/ascend/.
---

<objective>
Materializar a arquitetura modular em targets, headers publicos e sources reais.

Purpose: transformar a separacao conceitual definida nos docs em uma estrutura de filesystem e linkagem que o estudante consiga navegar e compilar.
Output: modulos `domain`, `content`, `session` e `adapters` criados sob `src/`.
</objective>

<execution_context>
@$HOME/.codex_sessions/pessoal/get-shit-done/workflows/execute-plan.md
@$HOME/.codex_sessions/pessoal/get-shit-done/templates/summary.md
</execution_context>

<context>
@CMakeLists.txt
@CMakePresets.json
@src/CMakeLists.txt
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bootstrap-da-fundacao-c-1x1/02-CONTEXT.md
@.planning/phases/02-bootstrap-da-fundacao-c-1x1/02-RESEARCH.md
@docs/engineering/backend-cpp-foundation.md
@docs/combat-1x1/combat-domain.md
@docs/combat-1x1/attributes-and-skills.md
</context>

<threat_model>
<trust_boundaries>
- `domain` nao pode depender de `session` nem de `adapters`.
- O placeholder desta phase nao pode virar implementacao prematura de `CombatState` ou do catalogo canonico; ele serve para provar estrutura, nao funcionalidade do duelo.
- O adapter textual precisa existir como borda reservada, sem empurrar regra de dominio para fora do core.
</trust_boundaries>

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Module boundaries | mitigate | Criar um target por modulo, com `include/ascend/...` e um source real por target; nao concentrar tudo em um target unico. |
| T-02-05 | Elevation of Privilege | Dependency direction | mitigate | Declarar em `target_link_libraries` que `ascend_session` depende de `ascend_domain` e `ascend_content`, e que `ascend_adapters` depende de `ascend_session`, sem links reversos. |
| T-02-06 | Repudiation | Text adapter reservation | mitigate | Expor um placeholder textual explicito via `ascend::adapters::text_adapter_name()` para registrar a borda futura do harness textual. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Criar modulos domain e content com API publica minima</name>
  <files>src/domain/CMakeLists.txt, src/domain/include/ascend/domain/module_name.hpp, src/domain/src/module_name.cpp, src/content/CMakeLists.txt, src/content/include/ascend/content/module_name.hpp, src/content/src/module_name.cpp</files>
  <read_first>CMakeLists.txt, src/CMakeLists.txt, src/domain/CMakeLists.txt, src/content/CMakeLists.txt, .planning/phases/02-bootstrap-da-fundacao-c-1x1/02-CONTEXT.md, .planning/phases/02-bootstrap-da-fundacao-c-1x1/02-RESEARCH.md, docs/engineering/backend-cpp-foundation.md</read_first>
  <action>Criar `src/domain/CMakeLists.txt` com `add_library(ascend_domain STATIC src/module_name.cpp)`, `target_include_directories(ascend_domain PUBLIC include)`, e `target_link_libraries(ascend_domain PUBLIC ascend_build_options ascend_build_warnings)`. Criar `src/domain/include/ascend/domain/module_name.hpp` declarando `std::string_view module_name();` em `namespace ascend::domain`, e `src/domain/src/module_name.cpp` retornando exatamente `"domain"`. Repetir o mesmo padrao para `content` com target `ascend_content`, header `src/content/include/ascend/content/module_name.hpp`, source `src/content/src/module_name.cpp` e retorno exatamente `"content"`.</action>
  <verify>test -f src/domain/CMakeLists.txt && test -f src/domain/include/ascend/domain/module_name.hpp && test -f src/domain/src/module_name.cpp && test -f src/content/CMakeLists.txt && test -f src/content/include/ascend/content/module_name.hpp && test -f src/content/src/module_name.cpp && rg -n "add_library\\(ascend_domain STATIC src/module_name.cpp\\)|target_link_libraries\\(ascend_domain PUBLIC ascend_build_options ascend_build_warnings\\)" src/domain/CMakeLists.txt && rg -n "add_library\\(ascend_content STATIC src/module_name.cpp\\)|target_link_libraries\\(ascend_content PUBLIC ascend_build_options ascend_build_warnings\\)" src/content/CMakeLists.txt && rg -n "namespace ascend::domain|std::string_view module_name\\(\\)" src/domain/include/ascend/domain/module_name.hpp && rg -n 'return \"domain\";' src/domain/src/module_name.cpp && rg -n 'return \"content\";' src/content/src/module_name.cpp && cmake --preset dev && cmake --build --preset dev --target ascend_domain ascend_content</verify>
  <acceptance_criteria>
    - src/domain/CMakeLists.txt contains `add_library(ascend_domain STATIC src/module_name.cpp)`
    - src/content/CMakeLists.txt contains `add_library(ascend_content STATIC src/module_name.cpp)`
    - src/domain/include/ascend/domain/module_name.hpp contains `namespace ascend::domain`
    - src/content/include/ascend/content/module_name.hpp contains `namespace ascend::content`
    - src/domain/src/module_name.cpp contains `return "domain";`
    - src/content/src/module_name.cpp contains `return "content";`
    - `cmake --build --preset dev --target ascend_domain ascend_content` exits 0
  </acceptance_criteria>
  <done>Modulos `domain` e `content` existem como targets independentes, com headers publicos e sources reais.</done>
</task>

<task type="auto">
  <name>Task 2: Criar modulos session e adapters com direcao de dependencias explicita</name>
  <files>src/session/CMakeLists.txt, src/session/include/ascend/session/module_name.hpp, src/session/src/module_name.cpp, src/adapters/CMakeLists.txt, src/adapters/include/ascend/adapters/text_adapter_name.hpp, src/adapters/src/text_adapter_name.cpp</files>
  <read_first>CMakeLists.txt, src/CMakeLists.txt, src/domain/CMakeLists.txt, src/content/CMakeLists.txt, src/session/CMakeLists.txt, src/adapters/CMakeLists.txt, .planning/phases/02-bootstrap-da-fundacao-c-1x1/02-CONTEXT.md, docs/engineering/backend-cpp-foundation.md</read_first>
  <action>Criar `src/session/CMakeLists.txt` com `add_library(ascend_session STATIC src/module_name.cpp)`, `target_include_directories(ascend_session PUBLIC include)` e `target_link_libraries(ascend_session PUBLIC ascend_build_options ascend_build_warnings ascend_domain ascend_content)`. Criar `src/session/include/ascend/session/module_name.hpp` declarando `std::string_view module_name();` em `namespace ascend::session`, e `src/session/src/module_name.cpp` retornando exatamente `"session"`. Criar `src/adapters/CMakeLists.txt` com `add_library(ascend_adapters STATIC src/text_adapter_name.cpp)`, `target_include_directories(ascend_adapters PUBLIC include)` e `target_link_libraries(ascend_adapters PUBLIC ascend_build_options ascend_build_warnings ascend_session)`. Criar `src/adapters/include/ascend/adapters/text_adapter_name.hpp` declarando `std::string_view text_adapter_name();` em `namespace ascend::adapters`, e `src/adapters/src/text_adapter_name.cpp` retornando exatamente `"text-adapter"`.</action>
  <verify>test -f src/session/CMakeLists.txt && test -f src/session/include/ascend/session/module_name.hpp && test -f src/session/src/module_name.cpp && test -f src/adapters/CMakeLists.txt && test -f src/adapters/include/ascend/adapters/text_adapter_name.hpp && test -f src/adapters/src/text_adapter_name.cpp && rg -n "add_library\\(ascend_session STATIC src/module_name.cpp\\)|target_link_libraries\\(ascend_session PUBLIC ascend_build_options ascend_build_warnings ascend_domain ascend_content\\)" src/session/CMakeLists.txt && rg -n "add_library\\(ascend_adapters STATIC src/text_adapter_name.cpp\\)|target_link_libraries\\(ascend_adapters PUBLIC ascend_build_options ascend_build_warnings ascend_session\\)" src/adapters/CMakeLists.txt && rg -n 'return \"session\";' src/session/src/module_name.cpp && rg -n 'return \"text-adapter\";' src/adapters/src/text_adapter_name.cpp && cmake --preset dev && cmake --build --preset dev --target ascend_session ascend_adapters</verify>
  <acceptance_criteria>
    - src/session/CMakeLists.txt contains `target_link_libraries(ascend_session PUBLIC ascend_build_options ascend_build_warnings ascend_domain ascend_content)`
    - src/adapters/CMakeLists.txt contains `target_link_libraries(ascend_adapters PUBLIC ascend_build_options ascend_build_warnings ascend_session)`
    - src/session/include/ascend/session/module_name.hpp contains `namespace ascend::session`
    - src/adapters/include/ascend/adapters/text_adapter_name.hpp contains `namespace ascend::adapters`
    - src/session/src/module_name.cpp contains `return "session";`
    - src/adapters/src/text_adapter_name.cpp contains `return "text-adapter";`
    - `cmake --build --preset dev --target ascend_session ascend_adapters` exits 0
  </acceptance_criteria>
  <done>Modulos `session` e `adapters` criados com linkagem unidirecional e placeholder textual explicito.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f src/domain/CMakeLists.txt`
- [ ] `test -f src/content/CMakeLists.txt`
- [ ] `test -f src/session/CMakeLists.txt`
- [ ] `test -f src/adapters/CMakeLists.txt`
- [ ] `rg -n "add_library\\(ascend_domain|add_library\\(ascend_content|add_library\\(ascend_session|add_library\\(ascend_adapters" src/*/CMakeLists.txt` succeeds
- [ ] `rg -n "ascend_domain ascend_content" src/session/CMakeLists.txt` succeeds
- [ ] `rg -n "ascend_session" src/adapters/CMakeLists.txt` succeeds
- [ ] `cmake --preset dev` succeeds
- [ ] `cmake --build --preset dev --target ascend_domain ascend_content ascend_session ascend_adapters` succeeds
</verification>

<success_criteria>

- All tasks completed
- O filesystem expone os quatro modulos escolhidos na discussao
- As dependencias internas aparecem no build de forma didatica
- O adapter textual fica reservado sem empurrar regra para fora do core
  </success_criteria>

<output>
After completion, create `.planning/phases/02-bootstrap-da-fundacao-c-1x1/02-02-SUMMARY.md`
</output>
